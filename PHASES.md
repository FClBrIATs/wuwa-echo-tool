# 改修フェーズ計画

対象: `echo-optimizer-v5.index.html`（最新版 5,379行）

Phase 1〜3 は相互に独立で、まとめて1リリースにできる。
Phase 5 は Phase 6 以降の前提。

---

## Phase 1 — サブステ正規データの反映【最優先】

公開された正規データにより、値テーブル自体の誤りが判明したため最優先。

### 内容
1. 値テーブルの修正（4箇所）
   - `def_pct` : 9.9,10.8,11.7,12.6,13.5 → 10.0,10.9,11.8,12.8,13.8
   - `flat_hp` : 400 → 390
   - `flat_atk`: [30,35,40,45,50,55,60] → [30,40,50,60]
   - `flat_def`: [30,35,40,45,50,55,60] → [40,50,60,70]
2. 抽選確率を整数重みで追加（丸め誤差ゼロ）
   - クリ率/クリダメ: [70,70,70,24,24,24,9,9] / 300
   - 8段の%系・共鳴効率・固定HP: [7,8,21,25,18,15,6,3] / 103
   - 固定攻撃力: [7,54,39,3] / 103
   - 固定防御力: [15,46,33,9] / 103
3. `dmg_echo` を SUB_STATS から削除し13種にする（種類の抽選は一様 1/13）
4. `expectedVal(sd)` を導入し `subMid()` を置換
5. モンテカルロの値抽選を重み付きに変更
6. データ出典をコメントに明記

### 完了条件
- 期待値がクリ率7.530 / クリダメ15.060 / 攻撃力%8.771 / 固定防御力53.495 と一致
- 最大値の出現確率がクリ系3.00% / %系2.91%
- ドロップダウンの選択肢がゲーム内の実値と完全一致

### 影響
現行はクリ系を他ステに対し約10%過大評価、理想ロールの出現確率を約4倍過大評価している。

---

## Phase 2 — 計算バグ修正

### 内容
1. バランス判定を `cr / (cd - 100)` に修正（`:3441`）
2. クリダメ未入力時のガード（既定150 もしくは警告して計算停止）（`:3281`）
3. クリ率100%付近のクリップ `Math.min(v, 100 - cr)`（`:3284`）

### 完了条件
- 70CR/240CD →「バランス良好」
- クリダメ空欄で `crit_factor` が0.3にならず、クリ率の限界価値が負にならない
- クリ率95%＋クリ率サブステ10.5% の評価が5%分に抑制される

---

## Phase 3 — データ保全

バックアップ機能が汚染データを撒く状態のため緊急度が高い。

### 内容
1. `addCharaEntry` / `deleteCharaEntry` の保存対象をユーザー登録分のみに限定（`:5297`, `:5318`）
2. 読み込み時に `_builtin` 付きエントリを除去するマイグレーション
3. `importState` に `Array.isArray` 検証と `_builtin` フィルタを追加
4. `__ver` の検証を実装（現在は書き出すだけで未チェック）
5. `saveEchoPresets` に try/catch（`:3744` 相当、他と非対称）

### 完了条件
- 10回登録しても固定プリセットは5件のまま
- 汚染済みJSONを復元しても重複が発生しない
- Safariプライベートモードで保存失敗時に無言で壊れない

---

## Phase 4 — ドキュメント整合と低コスト改善

### 内容
1. README修正
   - 「入力データは自動保存」→ 実態に合わせる（登録データのみ）
   - 「正規分布近似」→ モンテカルロ（Phase 6 で厳密化予定）
   - 対応ゲームバージョンを明記
2. バックアップモーダルに「対象は登録データのみ」と明示
3. `inputmode="decimal"` を数値入力129箇所へ一括付与
4. ダークモード（CSS変数17個は定義済み、`prefers-color-scheme` 追加のみ）
5. 死にコード削除: `normalCDF`, `collectRawStatsFields`, `onEchoMain2Key`,
   `onEchoMain`, `onEchoMainKey`, `autoBuffEnabled`, `stat_eff`
6. 書きかけコメント `}// 項目別` の除去
7. GitHub Pages 公開（ファイル名は既存URLを壊さないため `echo-optimizer-v5.index.html` のまま維持）

---

## Phase 5 — 状態の一元化【基盤】

現在 `S` / DOM / localStorage の三重管理。ここを直すと後続が大幅に楽になる。

### 内容
1. 単一 state → 一方向描画へ整理（`getPartials` のDOM直読みを排除）
2. ①②タブ入力の自動保存
3. 状態をURLハッシュに圧縮（バックアップ・共有・端末間移行が同時に解決）
4. キャラ単位のビルドプロファイル保存・切替
5. Undo（URL履歴 = ブラウザの戻る）
6. 計算ロジックをDOM非依存の純関数として分離しユニットテスト導入

### 完了条件
- リロードで全入力が復元
- URLを共有すると同じビルドが再現
- `cr / cd` 級のバグがテストで検出できる

---

## Phase 6 — 厳選ロジックの本体強化

Phase 1（重みテーブル）と Phase 5（テスト基盤）が前提。

### 内容
1. モンテカルロを動的計画法へ置換（重み付き・厳密・決定的）
2. 最終スコアの分布表示（中央値 / 上位25% / 下位25% / 現在のパーセンタイル）
3. 見切りライン表（「+5でX未満なら捨てる」「+10でY未満なら捨てる」を逆算）
4. スコアを厳密差分に変更（そのサブステを抜いた状態とのE差。25回計算するだけ）
5. ロール換算表示（「このサブステは1.4ロール分」）
6. 育成コスト（経験値・チューナー）を分母にした効率比較

### 完了条件
- 同一入力で確率が毎回同じ値
- 現行±0.4ptのブレが消える

---

## Phase 7 — 継続的改善

- 目標共鳴効率を入力させ、不足時のみ共鳴効率に価値を持たせる
- ハーモニー条件付き効果のアップタイム係数
- ステータス参照比率（攻撃力とHPの混合参照キャラ対応）
- ゲームデータのJSON外出し＋ビルド時埋め込み（データ更新PRを受けやすく）
- PWA化（オフライン・ホーム画面追加）
- ゲーム内実測値との突き合わせ表をREADMEに掲載（信頼性の証明＋回帰テスト）

---

## 見送り

- 敵防御・耐性・レベル差のモデル化: 音骸間の比較では定数倍としてしか効かず順位が変わらない
- onclick属性139箇所のイベント委譲化: 効果に対し改修量が大きい
- コスト構成・セット割当の最適化: 別軸の機能として切り離す
