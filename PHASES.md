# 改修フェーズ計画

対象: `echo-optimizer-v5.index.html`

レビューで見つかった不具合の修正と、その後の改善の記録です。
着手前の計画と実際にやったことがずれた箇所は、理由を添えて残しています。

---

## 完了

### Phase 1 — サブステ正規データの反映

出典：鳴潮（めいちょう）wiki のサブステータス抽選確率表。

値テーブルに4箇所の誤りがあった。

| 項目 | 修正前 | 修正後 |
|---|---|---|
| `def_pct` | 9.9, 10.8, 11.7, 12.6, 13.5 | 10.0, 10.9, 11.8, 12.8, 13.8 |
| `flat_hp` | 400 | 390 |
| `flat_atk` | 30, 35, 40, 45, 50, 55, 60 | 30, 40, 50, 60 |
| `flat_def` | 30, 35, 40, 45, 50, 55, 60 | 40, 50, 60, 70 |

固定攻撃力・固定防御力はゲーム上存在しない値（35/45/55）を選択肢に出していた。
固定防御力はテーブルごと1段ずれていた。

あわせて抽選確率を整数の重みで保持し（クリ率・クリダメは合計300、その他は103）、
値の抽選と1枠平均を重みに従わせた。従来の一様抽選は最高値を引く確率を
約4倍に過大評価していた（実際3.0%に対し12.5%）。

優先度ランキングが配列の中央要素を「典型的な1ロール」として使っていたため、
他のステータスに対しクリ率・クリダメを7.6%高く見積もっていた。重み付き期待値に変更。

サブステの種類は13種からの均等抽選（各7.6923%）なので、
音骸スキルダメージアップを `SUB_STATS` から除外した。

### Phase 2 — クリ率・クリダメの計算修正

- バランス判定を `cr / (cd - 100)` に修正。70CR/240CD という定石的なビルドが
  「クリダメ過多」と逆の助言を受けていた
- クリダメ未入力時は100を下限とし、警告を表示。従来は `crit_factor` が0.300になり、
  クリ率の限界価値が負に転じて優先度ランキングが逆転していた
- クリ率100%の頭打ちを考慮（この特別扱いは Phase 6 の厳密差分で不要になり撤去）

### Phase 3 — 固定プリセットの保存データ混入を修正

`getCharaEntries()` の戻り値をそのまま localStorage に書き戻していたため、
登録・削除のたびに固定プリセットが複製されていた（5件 → 1回登録で11件 → 2回で17件）。

保存経路を `getSavedCharaEntries()` / `saveCharaEntries()` に分離し、
起動時の `migrateCharaStorage()` で既存の汚染データを掃除する。
バックアップの書き出し・復元にも同じフィルタを適用した。

### Phase 4 — ドキュメントの実態合わせと整理

- README の「入力データは自動保存」「正規分布近似」がいずれも事実と異なっていたため修正
- `--text1` が未定義だったのを `--text` に
- 数値入力すべてに `inputmode="decimal"` を付与
- 未使用コードの削除（`normalCDF` ほか）
- GitHub Pages 用に転送ページ `index.html` を追加。
  本体のリネームは既存の共有リンクを壊すため見送った

### Phase 4.5 — 表示の不整合

- 確率まわりの説明文が Phase 1 以前の記述（「均等な確率で出現する」）のままだった
- 一度も入力していないスロットに「未入力」タグを表示。
  **未入力のサブステを価値0として計上する挙動は変更していない** —
  「不要なサブステは入力を省略する」という使い方があるため
- 登録名・メモを HTML に埋め込む際のエスケープを追加

### Phase 5 — 計算の状態集約とテスト基盤

**テスト基盤**：配布物の HTML をそのままブラウザで開いて検証する方式にした。
ソースを分割してビルドする案は、単一ファイルの編集フローが崩れるため採らなかった。

**項目別モード**：`getPartials()` が内訳を DOM から直接読んでいたため、
`fillDetailInputs()` がその経路で呼ばれたかどうかにスコアが左右されていた。
`detailBreakdown()` に一本化し、導出欄は読み取り専用にした。

**自動保存**：状態が集約されたので、`S` とモードフラグをまとめて保存・復元する。
壊れた保存データで起動できなくなるのが最も困るため、復元失敗時は初期状態から始める。

### Phase 6 — 厳密差分によるスコア

各サブステの限界価値を足し上げるとクリ率×クリダメの相乗効果を取りこぼし、
強い音骸ほど過大評価になっていた（最大ロールで+18.8%、平凡な5枠で+2.7%）。
ズレが一様でないためスロット間の順位が入れ替わりうる。

スロットのスコアを「そこを空にしたときに失われる期待ダメージ」に変更。
新規判定も同じ基準で測るようにして、②タブと③タブのスコアを直接比較できるようにした。

### Phase 7 — 可視化

**②装備音骸タブ：入れ替え難易度**
各スロットが「ランダムに拾った音骸の上位何%か」を表示。
スコアは貢献度しか答えないが、この数字は「引き直す価値があるか」に答える。
分布はスロットごとに作る（基準が違うため）。ビルドの署名でキャッシュし、
入力が止まってから計算する。

**③新規判定タブ：最終スコアの分布図**
「この音骸を育て切った場合」と「新しい音骸を+0から育てた場合」の分布を並べ、
合格ラインを重ねる。確率の数字だけでは、線が山のすぐ先にあるのか
遠い裾にあるのかが区別できない。

あわせてシミュレーションを厳密スコアの標本抽出に書き換えた。
従来は限界価値の足し上げで、Phase 6 で捨てたはずの線形近似が残っていた。

**乱数の固定**：mulberry32 の固定シードにした。従来は同じ入力でも確率が
±0.5pt 動き、標本誤差というより「ツールが自信なさげ」に見えていた。

### Phase 8 — 使い勝手

**厳選優先度を表形式に**
スコア・入れ替え難易度・サブステ一覧を1行にまとめた。
「スロット3が弱い」と読んでからカードまで目を移して理由を探す、という
二度手間を無くすため。価値0のサブステは破線・淡色で示す。
並びは弱い順のまま（このカードは「次に何を直すか」に答えるもの）。

**登録データの編集**
削除して打ち直すしかなく、キャラだと22項目の再入力になっていた。
一覧の「編集」でフォームに読み込み、IDを保ったまま上書きする。
①タブで選択中のデータを編集しても選択が外れない。

**属性ダメバフの保存先を修正**
登録フォームに属性ピッカーがあるのに、`readBuff()` が値を常に `dmg_all` へ
書いていた。焦熱バフとして登録したハーモニーが凝縮ビルドにも加算されていた。
既存データは自動変換していない（ピッカーの初期値が焦熱のため、
「全属性のつもり」か「焦熱のつもり」か判別できない）。
該当エントリには一覧で警告を出し、編集機能で直せるようにした。

**音骸カードを3列2段に**
横1列だとサブステの選択欄が窮屈だった。

**キーボード操作**
到達できない操作は2種類だけだった。ハーモニー選択肢を `<button>` に変更し、
モーダルは Esc で閉じられるようにした。フォーカスリングは
`:focus-visible` でキーボード操作時のみ表示する。

### Phase 9 — 入力コストの削減

ドロップダウンは「種類」と「値」で2回選ぶ必要があり、装備5スロットで60回かかっていた。
方式を比べる試作（`input-proto.html`）を作り、マトリクス方式を採用。

**サブステの入力パネル**
種類×値の表を出し、交差セルを1回押すと両方決まる。押したら次の空き枠へ自動で送るので、
パネルを1度開けば5枠を5タップで埋められる。セルの色は今のビルドでのロールの価値で、
入力しながら当たり外れが分かる。②装備音骸タブと③新規判定タブで共通。

**コストとメインステ**
メインステはコストで決まり、組み合わせは全19通りしかない。別々に選ばせず1タップにした。
あわせてコスト合計を表示する（上限12、実際の構成は 4-3-3-1-1 か 4-4-1-1-1 の2択）。
以前は5枠すべてコスト4のような、ゲーム上あり得ない構成でも計算できていた。

**ダメージ比率のプリセット**
5つの数値を合計100になるよう手入力させていた。よくある構成をボタンにし、
押したあと個別に微調整もできるようにした。

**新規音骸の入力が状態に入った**
③タブのサブステは DOM にしか無く、自動保存の対象外だった。
状態に移したことでリロードしても残る。

結果：1音骸あたり12タップ → 8タップ。5スロットで60 → 35。

### Phase 10 — 入力を項目別に一本化

「合計値入力」と「項目別入力」の2モードがあり、合計値モードは登録データを一切使わない
別系統の計算だった。同じ画面に2通りの入力があること自体が分かりにくく、
実際の使用では項目別しか使っていなかったため、合計値モードを削除した。

**合計欄が入力も受ける**
合計値モードで得られていたのは「ゲーム内の数字をそのまま打てる」ことなので、
これは項目別のまま残した。各項目の合計を大きく表示し、そこに直接打ち込むと
`直接入力 = 打った値 − 自動計算されている分` が『直接入力』欄に入る。

合計を固定値として別に持つ案は採らなかった。そうするとハーモニーを切り替えても
合計が動かず、内訳と合計が食い違った状態になる。差分として持てば、
あとから登録データを足しても合計が正しく追従する。
自動計算の分が打った値を上回る場合は差分が負になるが、これは
「ハーモニーを選んだが、打った合計にはその分が入っていない」という正しい状態なので、
そのまま持たせて注釈で示す。

**その他 → 直接入力**
導出欄が読み取り専用の自動計算になったので、対になる欄の名前は
「何が入るか」ではなく「誰が入れるか」を表したほうが分かる。

**手入力と自動計算の区別**
導出欄は点線・淡色にしていたが、実線との差が小さく、どこを自分で埋めるのか
一目で分からなかった。手入力の欄に色を付け、見出しに ✎ を付けて、
内訳の先頭に凡例を出す。色だけに頼らず枠線の種類（点線／実線）でも区別する。

**選択式の入力をまとめる**
ハーモニー選択はキャラ・武器と同じ「登録データから選ぶ入力」なのに、
ダメージ比率の後ろに離れて置かれていた。キャラ・武器と同じ枠へ移し、
数値の内訳より前に出した。

なお、この位置だと `.sec-title` が `.card-body` の直下ではなくなるため、
折りたたみへの変換対象から外れる。一次入力が畳まれないという意味では
都合が良いが、暗黙の依存なので回帰ケースで固定してある。

**ついでに直した**
- `resetStatsTab()` のセレクタが `input[type="number" inputmode="decimal"]` という
  不正な形になっており、`querySelectorAll` が例外を投げてリセットが丸ごと動いていなかった
- ハーモニー選択に見出しが無く、直前の「ダメージ比率」の折りたたみに巻き込まれて
  ページから見えなくなっていた（上記の移動で解消）

移行処理は入れていない。合計値モードの入力は `S.other` へ機械的に移せる
（実測で期待ダメージ指数が完全一致することを確認済み）が、実使用が無いため
不要と判断した。保存データは項目別の入力をそのまま読み込む。

### Phase 11 — 白基調へ戻す

配色を暗地に変えたことで、金色が「見出し・スコア・手入力欄・合計」の4つの意味を
同時に持つ状態になり、強調が効かなくなっていた。7/8 時点の白基調へ戻す。

`:root` の差し替えだけでは戻らない箇所を測って直した。

| 直したもの | 理由 |
|---|---|
| 属性6色 | 回折 `#eab308` が白地で 1.9:1。6色とも濃い版へ |
| 入れ替え難易度の段階色 | 暗地用に「暗→明」で作ってあり、白地では最上位が消える。「淡→濃」へ反転 |
| 分布図の `--dc-dim` / `--dc-line` | 同上。沈める側が白地では逆に目立っていた |
| `#0a0b0e`（11箇所） | 「アクセント色のボタンに載る文字」に背景色を流用していた。`--on-p` を用意 |
| 情報ボックスのシアン | 淡いシアン地に淡いシアン文字で、白地では読めない |
| 発光表現（text-shadow / 0 0 Npx の box-shadow） | 暗地の表現。白地では滲みか、フォーカスリングの誤認になる |

あわせて、コントラスト不足だった箇所を測定に基づいて直した。
補助文字（9〜10px）が 3.45:1 と、ページで一番小さい文字が一番薄い状態だった。
入力欄の点線は 1.33:1 でほぼ見えておらず、「点線＝自動計算」という区別自体が
成立していなかった。UI の境界線には 3:1 を満たす `--ui-line` を用意した。

「明るいセルほど価値が高い」といった明暗前提の説明文も書き換えた。

回帰ケースは、表示中の全要素のコントラストを実測して4.5:1（大きい文字は3:1）を
下回るものが無いことを確認する形にした。目視では見落とすため。

**テストが詰まっていた問題もここで直した**
本体は Google Fonts を `<link>` で読む。読み込みが詰まると後ろの `<script>` の実行まで
止まるため、1ページあたり最大30秒待たされてテスト全体が時間切れになっていた。
テストではフォントを取りに行かせないようにした。

### Phase 12 — 収録データの拡充・登録タブの不具合修正

**キャラ43体・武器45本を収録**
以前はキャラ4件・武器5件のみで、拡充には正確なゲーム内数値の出典が必要だった。
鳴潮wiki(wikiwiki.jp/w-w/)を参照して収集したデータを取り込んだ。全バフ発動時・
レベル90・共鳴チェーン0(S0)の値。共鳴チェーン・HP回復効果%・共鳴効率%は未対応のため
含まれない（ヒーラーのステータスボーナスの一部が反映されない状態は変わらず）。

デュアルタイプ（複数のダメージ種が同格で主力になるキャラ）は `ratio` の合計を200%
（両方に満額の重みを付ける）で登録した。大雑把な音骸厳選という用途では、実際の配分比
（例えば60/40）を調べて按分するより実態に近いという判断。純ヒーラー3体（モーニエ・
ショアキーパー・ヴェリーナ）は `ratio: {}`（全項目0、直接ダメージへの寄与なし）とした。

ルシラーの限定武器として伝わっていた「フリーズフレームR1」は、出典側が
「ダーニャの偽物の矮星との混同の可能性、要確認」と明記していたため見送った。
裏取りできるまで収録しない。

**ダメージ比率の「合計100%」警告が登録キャラで誤爆する問題**
デュアルタイプ（合計200%）・純ヒーラー（合計0%）を登録した結果、これらのキャラを
選ぶたびに「⚠ 合計が100%になっていません」という警告が常時出るようになった。
警告自体は正しい配分に対して出ているので、登録キャラ由来のratioかどうかを
`ratioSource` で覚えておき、その場合だけ警告を止めるようにした（手で1項目でも
編集すれば通常の警告に戻る）。

**武器に武器種別（`weaponType`）を追加**
長刃・迅刀・拳銃・手甲・増幅器の5種。グリッド選択パネル（後述、未着手）でタブの軸に
使う想定。一覧にも目印として表示する。

**登録タブでハーモニーの固定プリセットが表示されない不具合**
`renderCustomList()` がユーザー登録分（`customHarmonies()`）しか見ておらず、
①タブのピッカーには30件出るのに登録タブには1件も出ない状態だった。
キャラ・武器と同じ「固定バッジを付けて編集・削除ボタンは出さない」形に揃えた。

### Phase 13 — ハーモニー・キャラ・武器選択のグリッドパネル化

ハーモニー30件・キャラ43件・武器45件と、ドロップダウンの縦一列リストでは
目当ての1件を探しにくい件数になっていた。属性 or 武器種別のタブで絞ってから、
名前だけのグリッドから選ぶ形式に変更した。

- ハーモニー・キャラ：属性タブ（焦熱・凝縮・電導・気動・回折・消滅・その他）
- 武器：武器種別タブ（長刃・迅刀・拳銃・手甲・増幅器・その他）。「その他」は
  `weaponType` が無い場合の受け皿（ユーザーが登録した古い武器データなど）

サブステ入力のマトリクス（Phase 9）とは軸の意味が違う。マトリクスは
「種類×値」の2軸を1タップに潰すためのものだったが、ハーモニー・キャラ・武器の
選択は元々1タップなので、タップ数を減らす効果は無い。ここでの狙いは
「件数が増えても探しやすいこと」で、効くのは絞り込みの軸であって
マトリクス化そのものではない。

セルは名前のみ表示（属性アイコン等は付けない）。タブとグリッドの組み合わせを
3箇所（ハーモニー2枠・キャラ・武器）で共通の関数 `gpkPanelHTML()` から作る。
選択の内部状態は引き続き隠しselect（`sel_chara`/`sel_weapon`）で持ち、
`getCharaWeaponBuff()` 等の既存コードは変更していない。

**位置の調整**：パネル幅がトリガーボタンより広いため、狭い画面でトリガーが
画面の左寄りに無いと右にはみ出すことがあった。開いた時点で画面右端を
超えるかどうかを測り、超える場合だけ右揃えに切り替える。

### Phase 14 — ハーモニーの収録データ拡充（Ver3.4〜3.5分）

既存30種と鳴潮wikiの一覧（全34種）を突き合わせ、未収録だった4種を追加した。

- **冥夜を導く灯・煞を祓う浄心**：既存の書式にそのまま当てはめられる2種
- **羽舞う塵世の歌**：5セット効果が編成（虚滅編成／結霜編成）で別物になるため、
  1つのハーモニーとしてではなく用途ごとに別ID（`玄翎虚滅`/`玄翎結霜`）で登録した。
  2セット効果（共鳴効率+10%）はどちらも本ツール未対応のフィールドなので反映されない

**玄翎結霜の5セット効果**（「共鳴効率1%につきチーム攻撃力+0.1%、最大+25%」という
共鳴効率依存の効果）は、共鳴効率を直接モデル化できないため固定値で近似する必要が
あった。厳選ツールという用途上、共鳴効率は十分確保されている（バフが上限の+25%に
達している）前提で `atk_pct: 25` として登録した。実際の共鳴効率が低いキャラで使うと
過大評価になる。

**ナイトメア・スペクター**（1音骸のみで発動する新形式のセット効果）は見送った。
今のツールは「2セット/5セット」の2択（`setType`）しか扱えず、この形式を正確に
表現するには `S.harmony` を2枠→3枠に拡張する構造変更が要る。既存の保存データとの
互換性も絡むため、別タスクとして扱う。

### Phase 14.5 — フリーズフレームR1の裏取り

Phase 12では出典側が「ダーニャの偽物の矮星との混同の可能性、要確認」と注記して
いたため収録を見送っていた。ステータス画面のスクリーンショットで実測値が確認できた
ため追加した。武器効果「フェードアウト」は常時攻撃力+12%、結霜効果付与後に
自身の凝縮ダメージ+30%・チーム攻撃力+24%（ともに条件付き）。攻撃力%は常時分と
条件付き分を合算し `atk_pct: 36` として登録した。紐付くキャラ名はスクリーンショットに
無いため未確認のまま。

**偽物の矮星R1のスクリーンショット照合で欠落を発見・修正**：同じ流れでダーニャの
偽物の矮星R1のスクリーンショットも照合したところ、元データが常時分の攻撃力+12%しか
反映しておらず、条件付きのチーム攻撃力+24%が抜けていることが分かった。`atk_pct`を
12→36に修正した。

このミスは「共鳴解放ダメージ+36%」という目立つ効果の陰で、二段目の条件（チーム内の
別キャラが同条件を満たす）に紐づくチーム攻撃力バフが見落とされていた形。
フリーズフレームR1と同じパターンであり、**スクリーンショットで照合していない
残りの登録データにも同種の欠落がありうる**。この環境からはwikiに直接アクセスできず
（`wikiwiki.jp`は組織ポリシーで遮断）、全件を機械的に再照合する手段が無いため、
気になるキャラ・武器があれば都度スクリーンショットを見せてもらうのが確実。

### Phase 14.6 — 追加の資料照合

別途整理された修正資料と現在の収録データを突き合わせ、実際に差分のある箇所だけ
適用した（差分の大半は`weaponType`未収録によるノイズで、実質的な修正は3件）。

- **フィービーのratio**：`{heavy:100, skill:100}` → `{normal:100, heavy:100}`。
  「共鳴スキル：清浄なるコンフェッション」はモード切替の入力操作名であり
  ダメージ種別ではなく、実際の出力は通常攻撃＋重撃だった（武器「光のハルモニア」の
  対応バフとも整合）
- **氷華の雅印R1**：`dmg_normal` 12→52（未登場時の最大発動値。登場時のみなら36）
- **糸繰りの奇術R1**：`atk_pct` 0→24を追加（登場時の値。未登場時なら48）
- **夕霞の飲露R1**：紐付くキャラをルシラー→穂穂に訂正（穂穂は本データ収集時点で
  未実装のためキャラ未登録のまま）

**矛盾の扱い**：この資料は偽物の矮星R1・フリーズフレームR1のatk_pctを
元の値（12）のまま「直接確認済み」としていたが、Phase 14.5でスクリーンショットの
実測値から36に修正済みだった。この資料はキャラの紐付けを確認したもので、
効果の数値自体を再検証したものではなさそうだったため、**スクリーンショットという
一次証拠を優先し、36のまま据え置いた**。複数の情報源が食い違う場合は、
機械的にどちらかへ揃えるのではなく根拠の強さで判断する。

### Phase 14.7 — 3回目の資料照合：条件付きバフの見落とし14件

さらに詳細化された資料と突き合わせたところ、偽物の矮星R1・フリーズフレームR1と
同じパターン（目立つ効果の陰で二段目以降の条件付きバフが見落とされていた）が
広範囲に見つかった。実際に差分があった14件を修正した。

**単発の見落とし**（それまで0だった条件付きバフを追加）：
定めを覆す荊冠R1（`dmg_all`+20）、スターキャリブレーターR1（`crit_dmg`+20）、
闘炎の爪痕R1（`dmg_fire`+24）、破敵の鋭竹R1（`dmg_echo`+20）、
スペクトラル・トリガーR1（`dmg_light`+40）、スペクトル・ブラスターR1（`dmg_all`+24）、
デイブレイカースパインR1（`dmg_normal`+20）、夕霞の飲露R1（`atk_pct`+20）、
星々のコンチェルトR1（`atk_pct`+14）

**常時分＋条件付き分の合算漏れ**（偽物の矮星R1と同型のミス）：
スカル・ポッパーR1（`atk_pct`12→36、`dmg_normal`12→36）、
ボゾンの観測器R1（`atk_pct`12→24）

**スタック数の見落とし**（1スタック分だけを登録していた）：
赫く燃ゆる流光R1（`dmg_skill`4→56、最大14スタック）、
パルスの機腕R1（`dmg_normal`6→24、最大4スタック）、
光のハルモニアR1（`dmg_normal`/`dmg_heavy`14→42、最大3スタック）、
栄枯の湖岸R1（`dmg_normal`3.2→16、最大5スタック）

3回にわたる照合でこれだけの見落としが出てきたことを踏まえると、収録データ全体の
精度はまだ楽観できない。個々のスクリーンショット照合や、より詳細な資料との
突き合わせを続けるのが現実的な向き合い方。

### Phase 15 — 使い方テキストの現状反映・フッター追加

一連の改修（合計値入力の廃止、サブステのマトリクス入力、キャラ/武器/ハーモニーの
グリッド選択パネル化）で、各タブの「使い方」がそのつど更新されずに古い記述の
ままになっていた。

- ①タブ：キャラ/武器/ハーモニーのグリッド選択パネルについての説明を追加
- ②タブ：「サブステは種類→値の順に選択」という記述が、マトリクスから1タップで
  選ぶ現行の方式と合わなくなっていたため書き換え
- ③タブ：分布図（Phase 7で追加）についての説明が無かったため追加
- 登録タブ（武器・キャラ）：「①タブのドロップダウンから選択」という表現を
  「①タブの選択パネルから選ぶと」に修正
- コード内の古いコメント（合計値モードに触れていた1箇所）も削除

**フッターを全タブ共通にする**
クレジット表記が`#tab-custom`（登録タブ）の中にしか無く、他のタブでは見えなかった。
`.page`の外に出し、常に表示されるようにした。あわせて作者のTwitter（X）アカウントへの
リンクを追加した。

### Phase 16 — 各サブステ優先度の表示修正・武器登録に武器種を追加

PR #14マージ後、ユーザーからのフィードバックで2件の不具合が見つかった。

**①タブ「各サブステ優先度」に属性バフが混ざっていた**
このランキングは「音骸のサブステ1枠分の期待値」で各項目を比較するもの。
属性バフ（焦熱/凝縮/…ダメバフ）は音骸のサブステとして出現しないため、
「参考値（サブステでは出ません）」と注記した上で表示していたが、注記があっても
ランキングに並んでいる時点で紛らわしい・不要と判断し、項目自体を削除した。
実装は`updateMVRanking()`内で攻撃力%の期待値を代表値として流用していた箇所を
そのまま削除するだけで済んだ。READMEの機能一覧の記述も「音骸のサブステとして
出現する項目のみが対象」に修正した。

なお「音骸スキルダメバフ」も同じ理由（サブステに存在しない）で参考値扱いに
なっていたが、続けてユーザーから同じ理由で削除の指示があったため、こちらも
`SKILL_TYPES`から除いた。これで「各サブステ優先度」に並ぶ項目はすべて実際に
音骸のサブステとして出現するものだけになった。

**武器登録フォームに武器種の入力欄が無かった**
Phase 13でキャラ/武器選択をグリッドパネル化した際、武器は`weaponType`フィールドで
タブ分け（長刃・迅刀・拳銃・手甲・増幅器）するようにしたが、これはビルトインデータ
（`BUILTIN_WEAPON`）にしか設定しておらず、ユーザーが手動登録する武器フォームには
武器種を選ぶ欄が無かった。キャラ登録フォームには属性選択ボタンがあるのと非対称な
状態だった。結果、登録した武器は常に`weaponType`未設定＝「その他」タブに入るしか
なく、武器種タブで絞り込む選択パネルの利点を活かせなかった。

キャラ登録の属性選択ボタン（`ch_attr_c` / `setCharaAttr`）と同じパターンで、
武器登録フォームに武器種選択ボタン（`ch_weaponType` / `setWeaponType`）を追加した。
`addCharaEntry()`での保存、`fillCharaForm()`での編集時の選択復元も対応させた。

---

## 見送った項目

計画時には挙げていたが、検討の結果やらないと判断したもの。

**動的計画法による厳密確率**
実測したブレが±0.5pt しかなく、決定性が得られれば目的は達成できた。
乱数の固定（10行程度）で代替。スコアが連続値なので、状態空間を作るには
区間に丸める必要があり「厳密」にはならなかったという事情もある。

**分布表示と見切りライン（「+10でX未満なら捨てる」）**
ツールが判断を指示する形になるため。分布と順位を見せて、判断は利用者に委ねる。

**URL共有・Undo・プロファイル**
自動保存が入った時点で URL 共有が解く問題が無くなった。
Undo は URL 履歴に乗せる前提だった。プロファイルはキャラ登録・武器登録の
ドロップダウンと音骸セット保存でほぼ賄えている。

**敵防御・耐性・レベル差のモデル化**
音骸間の比較では定数倍としてしか効かず、順位が変わらない。

**onclick 属性のイベント委譲化**
効果に対して改修量が大きい。

---

## 未着手

- 共鳴効率のスコア化（現在は全モードで0）
  — ダメージのスコア化ではなく、「目標共鳴効率を入力させ、不足分にのみ
    価値を持たせる」形の付加的な機能として検討中。目標値の妥当性はプレイ感覚の
    話なので方針の確認が要る
- ハーモニー条件付き効果のアップタイム係数
- サブステ価値のヒートマップ（`viz-sample.html` のサンプル4「サブステ1枠あたりの価値」）
  — ②③タブのサブステ選択パネルの色分けとは別物。あちらは「今スロットに何を
    入れるか選ぶための」その場限りの表示、こちらは13種×8段階を一度に並べて
    「このビルドで無駄になっているサブステの種類」を選択操作なしで俯瞰する
    サマリー表示。役割が重なる部分もあるため、実装するかは要検討
- 登録フォームのマークアップ重複解消（`ch_*` と `ch_*_c` の二重定義など）
- 1セット専用枠（`setType:'1'`）への対応。ナイトメア・スペクターの登録に必要。
  touch箇所は5つ（`S.harmony`初期値・`resetStatsTab()`・`getHarmonyTotal()`の
  ループ・`buildHarmonyPicker()`のループ・`loadState()`の互換チェック）で、
  想定していたより小さい変更で収まりそう。既存の保存データ（2枠）を
  読み込んだ際に3枠目を空で補う移行処理は要る

### 見送り確定

**HP回復効果%の登録・スコア化** — 不要と判断。ヒーラーのステータスボーナスの
一部（回復効果%分）は登録データに反映しない状態を維持する。

### 取り下げ

**厳選難易度表（目標スコアが何個に1個か）**
一度実装したが、入れ替え難易度が各行に出ている状態では、
すぐ上の表が答えた問いにもう一度答えるものになっていたため削除した。
